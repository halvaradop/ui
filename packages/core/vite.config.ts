import { defineConfig } from "vite"
import { defaultConfig } from "./src/vite.config.default"

/**
 * Vite configuration for the @halvaradop package. This configuration uses
 * the default configuration provided by this package.
 */
export default defineConfig(defaultConfig)
